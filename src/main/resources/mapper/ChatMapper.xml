<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.test.demo.mapper.ChatMapper">
    <insert id="insertMessage" parameterType="com.test.demo.vo.ChatVO">
        INSERT INTO chat (type, room_id, sender, message, time)
        VALUES (#{type}, #{roomId}, #{sender}, #{message}, #{time})
    </insert>

    <select id="selectMessagesByRoomId" resultType="com.test.demo.vo.ChatVO">
        SELECT * FROM chat WHERE room_id = #{roomId}
    </select>

    <delete id="deleteMessage" parameterType="long">
        DELETE FROM chat WHERE id = #{id}
    </delete>


</mapper>