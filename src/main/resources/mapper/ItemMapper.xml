<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.test.demo.dao.ItemDao">

    <!-- 상품 데이터 삽입 -->
    <insert id="insertItem" parameterType="com.test.demo.vo.ItemVo">
        INSERT INTO item (
            item_name, category, title, seller_id, buyer_id, view, trade, status, price, my_price, note,
            link, image, author, publisher, description, reg_date, mod_date
        ) VALUES (
                     #{itemName}, #{category}, #{title}, #{sellerId}, #{buyerId}, #{view}, #{trade}, #{status}, #{price}, #{myPrice}, #{note},
                     #{link}, #{image}, #{author}, #{publisher}, #{description}, NOW(), NOW()
                 )
    </insert>

    <!-- 상품 데이터 조회 -->
    <resultMap id="itemResultMap" type="com.test.demo.vo.ItemVo">
        <result property="itemId" column="item_id"/>
        <result property="title" column="title"/>
        <result property="itemName" column="item_name"/>
        <result property="category" column="category"/>
        <result property="sellerId" column="seller_id"/>
        <result property="buyerId" column="buyer_id"/>
        <result property="view" column="view"/>
        <result property="trade" column="trade"/>
        <result property="status" column="status"/>
        <result property="price" column="price"/>
        <result property="myPrice" column="myprice"/>
        <result property="link" column="link"/>
        <result property="image" column="image"/>
        <result property="note" column="note"/>
        <result property="author" column="author"/>
        <result property="publisher" column="publisher"/>
        <result property="description" column="description"/>
        <result property="regDate" column="reg_date"/>
        <result property="modDate" column="mod_date"/>
    </resultMap>

    <select id="selectItems" resultMap="itemResultMap">
        SELECT item_id, item_name, title,category, seller_id, buyer_id, view, trade, status, price, my_price,
            link, image, author, publisher, description, reg_date, mod_date, note
        FROM item
        ORDER BY reg_date DESC
            LIMIT #{offset, jdbcType=INTEGER}, #{limit, jdbcType=INTEGER}
    </select>


    <!-- 특정 상품 데이터 조회 -->
    <resultMap id="itemVoMap" type="com.test.demo.vo.ItemVo">
        <result property="itemId" column="item_id"/>
        <result property="itemName" column="item_name"/>
        <result property="category" column="category"/>
        <result property="title" column="title"/>
        <result property="sellerId" column="seller_id"/>
        <result property="buyerId" column="buyer_id"/>
        <result property="view" column="view"/>
        <result property="trade" column="trade"/>
        <result property="status" column="status"/>
        <result property="price" column="price"/>
        <result property="myPrice" column="my_price"/>
        <result property="link" column="link"/>
        <result property="image" column="image"/>
        <result property="author" column="author"/>
        <result property="publisher" column="publisher"/>
        <result property="description" column="description"/>
        <result property="regDate" column="reg_date"/>
        <result property="modDate" column="mod_date"/>
        <result property="note" column="note"/>
    </resultMap>

    <select id="selectItemDetail" parameterType="int" resultMap="itemVoMap">
        SELECT
            item_id, item_name, category, title, seller_id, buyer_id, view, trade, status, price, my_price,
            link, image, author, publisher, description, reg_date, mod_date, note
        FROM item
        WHERE item_id = #{itemId}
    </select>


    <!-- 전체 상품 개수 조회 -->
    <select id="selectTotalItemCount" resultType="int">
        SELECT COUNT(*) FROM item
    </select>

    <!-- 페이지네이션을 지원하는 상품 조회 -->
    <select id="selectItemsWithPagination" resultType="com.test.demo.vo.ItemVo">
        SELECT
            item_id, item_name, category, seller_id,title, buyer_id, view, trade, status, price, my_price,
            link, image, author, publisher, description, reg_date, mod_date, note
        FROM item
        ORDER BY reg_date DESC
        LIMIT #{offset}, #{limit}
    </select>

    <!-- 상품 데이터 수정 -->
    <update id="updateItem" parameterType="com.test.demo.vo.ItemVo">
        UPDATE item
        SET
            item_name = #{itemName}, category = #{category}, seller_id = #{sellerId}, buyer_id = #{buyerId},
            view = #{view}, trade = #{trade}, status = #{status}, price = #{price}, my_price = #{myPrice},
            link = #{link}, image = #{image}, author = #{author}, publisher = #{publisher},note = #{note},title = #{title},
            description = #{description}, reg_date =#{regDate}, mod_date = NOW()
        WHERE item_id = #{itemId}
    </update>

    <!-- 상품 데이터 삭제 -->
    <delete id="deleteItem" parameterType="int">
        DELETE FROM item
        WHERE item_id = #{itemId}
    </delete>

    <!-- 조회수 증가 -->
    <update id="increaseView" parameterType="int">
        UPDATE item
        SET view = view + 1
        WHERE item_id = #{itemId}
    </update>

    <!-- 조회수 가져오기 -->
    <select id="getViewCount" parameterType="int" resultType="int">
        SELECT view
        FROM item
        WHERE item_id = #{itemId}
    </select>

    <!-- 찜 추가 -->
    <insert id="addZzim" parameterType="map">
        INSERT INTO zzim (item_id, buyer_id)
        VALUES (#{itemId}, #{buyerId})
    </insert>

    <!-- 상품 정가 가져오기 -->
    <select id="getPrice" parameterType="int" resultType="int">
        SELECT price
        FROM item
        WHERE item_id = #{itemId}
    </select>

    <!-- 카테고리 이름 가져오기 -->
    <select id="getCategoryName" parameterType="int" resultType="string">
        SELECT category_name
        FROM category
        WHERE category_id = #{categoryId}
    </select>

    <!-- 판매 게시물 등록 -->
    <insert id="insertPost" parameterType="com.test.demo.vo.PostVo">
        INSERT INTO post (
            post_title, item_id, seller_id, price, description, notes, reg_date, mod_date
        ) VALUES (
                     #{postTitle}, #{itemId}, #{sellerId}, #{price}, #{description}, #{notes}, NOW(), NOW()
                 )
    </insert>

    <!-- 판매 게시물 목록 조회 -->
    <select id="selectPosts" resultType="com.test.demo.vo.PostVo" parameterType="map">
        SELECT post_id, post_title, item_id, seller_id, price, description, notes, reg_date, mod_date
        FROM post
        ORDER BY reg_date DESC
        LIMIT #{start, jdbcType=INTEGER}, #{display, jdbcType=INTEGER}
    </select>

    <!-- 판매 게시물 상세 조회 -->
    <select id="selectPostDetail" parameterType="int" resultType="com.test.demo.vo.PostVo">
        SELECT post_id, post_title, item_id, seller_id, price, description, notes, reg_date, mod_date
        FROM post
        WHERE post_id = #{postId}
    </select>

    <!-- 판매 게시물 수정 -->
    <update id="updatePost" parameterType="com.test.demo.vo.PostVo">
        UPDATE post
        SET post_title = #{postTitle}, price = #{price}, description = #{description},
            notes = #{notes}, mod_date = NOW()
        WHERE post_id = #{postId}
    </update>

    <!-- 판매 게시물 삭제 -->
    <delete id="deletePost" parameterType="int">
        DELETE FROM post
        WHERE post_id = #{postId}
    </delete>

</mapper>
