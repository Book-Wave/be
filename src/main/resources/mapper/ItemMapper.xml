<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.test.demo.dao.ItemDao">

    <!-- 상품 목록 조회 -->
    <select id="selectItems" parameterType="map" resultType="com.test.demo.vo.ItemVo">
        SELECT * FROM items
        WHERE name LIKE CONCAT('%', #{search}, '%')
        ORDER BY ${sortField} ${sortOrder}
        LIMIT #{offset}, #{limit}
    </select>

    <!-- 특정 상품 상세 정보 조회 -->
    <select id="selectItemDetail" parameterType="int" resultType="com.test.demo.vo.ItemVo">
        SELECT * FROM items WHERE id = #{itemId}
    </select>

    <!-- 상품 등록 -->
    <insert id="insertItem" parameterType="com.test.demo.vo.ItemVo">
        INSERT INTO items (name, publisher, author, price)
        VALUES (#{name}, #{publisher}, #{author}, #{price})
    </insert>

    <!-- 상품 수정 -->
    <update id="updateItem" parameterType="com.test.demo.vo.ItemVo">
        UPDATE items
        SET name = #{name}, publisher = #{publisher}, author = #{author}, price = #{price}
        WHERE id = #{id}
    </update>

    <!-- 상품 삭제 -->
    <delete id="deleteItem" parameterType="int">
        DELETE FROM items WHERE id = #{itemId}
    </delete>

</mapper>
